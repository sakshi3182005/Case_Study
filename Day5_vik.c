void readStudent(Student std[],int n)
{
	printf("Enter the details(Rollno,Name,Maths marks,Physics marks,Chemistry marks,CS marks)";
	for(int i=0; i<n; i++) 
	{
		scanf("%d%s%d%d%d%d",&std[i].rollno,std[i].name,&std[i].maths marks,std[i].physics marks,std[i].chemistry marks,std[i].cs marks);
	}
}// Reading the student details
